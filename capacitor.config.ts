import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mi-prueba-ionic-con-jsx',
  webDir: 'dist'
};

export default config;
